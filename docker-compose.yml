services:
  backend:
    build:
      context: .
      dockerfile: backend-python/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./backend-python:/app:ro

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend